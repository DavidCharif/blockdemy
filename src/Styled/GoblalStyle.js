import { createGlobalStyle } from "styled-components";

export const GoblalStyle = createGlobalStyle`
  * {
    font-size: 16px;
    font-family: "Play", sans-serif;
    scroll-behavior: smooth;
  }
`